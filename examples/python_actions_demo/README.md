# INTENTO Python Actions Demo

This project demonstrates INTENTO Runtime v1.1 local Python registered actions.

Run it with:

```bash
python3 -m intento_runtime run examples/python_actions_demo --allow-python-actions
```

Expected output:

```text
INTENTO Runtime v1.1
INTENTO RUNTIME V1.1!
```
